{
    "tasks": [
        {
            "type": "shell",
            "label": "cmake-build",
            "command": "cmake -DCMAKE_PREFIX_PATH=/usr/ -B build .; cmake --build build -j8",
            "options": {
                "cwd": "${workspaceFolder}"
            },
        }
    ],
    "version": "2.0.0"
}